# Temporary build cache from Vite that shouldn't be uploaded
node_modules/.vite

# Repository metadata and local files that Vercel doesn't need
.git
*.log
.DS_Store

# Keep source and configuration files available for the build step.
# (Previously, entries like `src/` or `tsconfig.json` here caused
# the deployment to miss critical files and fail during `npm run build`.)
